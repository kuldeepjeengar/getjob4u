# getjob4u environment variables — copy to .env and fill in real values.
# .env is gitignored. Never commit real credentials.

# Admin login (used by the /admin dashboard)
GETJOB4U_ADMIN_USER=admin
GETJOB4U_ADMIN_PASS=change-me-to-a-strong-password
